{
  "name": "right2know-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install",
    "start": "cd server && npm start",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0"
  }
}
